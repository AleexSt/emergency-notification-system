version: '3.8'

services:
  # PostgreSQL для template-service
  template-service-postgres:
    container_name: template-service-postgres
    hostname: template-service-postgres
    image: postgres:alpine3.17
    restart: always
    ports:
      - '5434:5432'  # Ваш application.yml использует 5434
    environment:
      POSTGRES_DB: template
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      PGDATA: /data
    volumes:
      - db-postgres-template:/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - template-network





  recipient-service-postgres:
    container_name: recipient-service-postgres
    hostname: recipient-service-postgres
    image: postgres:alpine3.17
    restart: always
    ports:
      - '5435:5432'
    environment:
      POSTGRES_DB: recipient
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      PGDATA: /data
    volumes:
      - db-postgres-recipient:/data

  security-service-postgres:
    container_name: security-service-postgres
    hostname: security-service-postgres
    image: postgres:alpine3.17
    restart: always
    ports:
      - '5436:5432'
    environment:
      POSTGRES_DB: security
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      PGDATA: /data
    volumes:
      - db-postgres-security:/data

  notification-service-postgres:
    container_name: notification-service-postgres
    hostname: notification-service-postgres
    image: postgres:alpine3.17
    restart: always
    ports:
      - '5432:5432'
    environment:
      POSTGRES_DB: notification
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      PGDATA: /data
    volumes:
      - db-postgres-notification:/data

  zookeeper:
    image: confluentinc/cp-zookeeper:7.3.2
    container_name: zookeeper
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"

  kafka:
    image: confluentinc/cp-kafka:7.3.2
    container_name: broker
    ports:
      - "9092:9092"
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    depends_on:
      - zookeeper
  # Zookeeper (нужен для Kafka)
#  zookeeper:
#    image: confluentinc/cp-zookeeper:7.3.2
#    container_name: zookeeper
#    environment:
#      ZOOKEEPER_CLIENT_PORT: 2181
#      ZOOKEEPER_TICK_TIME: 2000
#    ports:
#      - '2181:2181'
#    networks:
#      - template-network
    # Kafka
#  broker:
#    image: confluentinc/cp-kafka:7.3.2
#    container_name: broker
#    ports:
#      - "9092:9092"   # Ваш application.yml использует localhost:9092
#      - "29092:29092" # Для внутренних подключений
#    depends_on:
#      - zookeeper
#    environment:
#      KAFKA_BROKER_ID: 1
#      KAFKA_ZOOKEEPER_CONNECT: 'zookeeper:2181'
#      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,PLAINTEXT_INTERNAL:PLAINTEXT
#      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092,PLAINTEXT_INTERNAL://broker:29092
#      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
#      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
#      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
#    healthcheck:
#      test: ["CMD", "kafka-topics", "--bootstrap-server", "localhost:9092", "--list"]
#      interval: 30s
#      timeout: 10s
#      retries: 3
#    networks:
#      - template-network

#  # Опционально: Kafka UI для удобства (http://localhost:8085)
#  kafka-ui:
#    image: provectuslabs/kafka-ui:latest
#    container_name: kafka-ui
#    ports:
#      - "8085:8080"
#    environment:
#      KAFKA_CLUSTERS_0_NAME: local
#      KAFKA_CLUSTERS_0_BOOTSTRAPSERVERS: broker:29092
#      KAFKA_CLUSTERS_0_ZOOKEEPER: zookeeper:2181
#    depends_on:
#      - broker
#    networks:
#      - template-network

  # Ваш Spring Boot приложение (если хотите запускать в Docker)
  # Раскомментируйте, когда захотите запустить и приложение в Docker
  # template-service:
  #   build: .
  #   container_name: template-service
  #   restart: always
  #   ports:
  #     - "0:1003"  # random port или фиксированный
  #   environment:
  #     - SPRING_PROFILES_ACTIVE=docker
  #   depends_on:
  #     - template-service-postgres
  #     - broker
  #   networks:
  #     - template-network

volumes:
  db-postgres: { }
  db-postgres-security: { }
  db-postgres-template: { }
  db-postgres-recipient: { }
  db-postgres-notification: { }

networks:
  template-network:
    driver: bridge